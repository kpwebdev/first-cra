// JSX => declarative code
// Vanilla Javascript => create, select, append, update, replace => imperative code

function FirstPage() {
  return (
    <div>
      <section id="hero">I am hero section</section>
      <header>Logo</header>
      <footer>I am the footer</footer>
    </div>
  );
}

export default FirstPage;
